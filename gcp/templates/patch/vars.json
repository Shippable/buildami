{
  "service_account_json": "${SERVICE_ACCOUNT_JSON}",
  "region": "${REGION}",
  "ssh_username": "${SSH_USER}",
  "machine_type": "${MACHINE_TYPE}",
  "zone":"${ZONE}",
  "project_id":"${PROJECT_ID}",
  "FAM_NAME":"${FAM_NAME}",
  "SOURCE_IMAGE_FAMILY":"${FAM_NAME}",
  "ARCHITECTURE":"${ARCHITECTURE}",
  "OS":"${OS}",
  "DOCKER_VER":"${DOCKER_VER}",
  "REQPROC_IMAGE":"${REQPROC_IMAGE}",
  "REQKICK_DOWNLOAD_URL":"${REQKICK_DOWNLOAD_URL}",
  "CEXEC_DOWNLOAD_URL":"${CEXEC_DOWNLOAD_URL}",
  "REPORTS_DOWNLOAD_URL":"${REPORTS_DOWNLOAD_URL}"
}
